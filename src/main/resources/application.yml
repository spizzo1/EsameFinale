server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    url: jdbc:postgresql://localhost:5432/Esame
    password: data123
  jpa:
    show-sql: 'true'
    hibernate:
      ddl-auto: create
